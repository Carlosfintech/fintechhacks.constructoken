# ============================================================================
# CONFIGURACIÓN DE RAFIKI
# ============================================================================

# URL de la API Admin de Rafiki (GraphQL)
RAFIKI_ADMIN_URL=http://localhost:3001/graphql

# URL del servidor de autorización de Rafiki
RAFIKI_AUTH_URL=http://localhost:3006

# URL del servidor de recursos de Rafiki (Open Payments)
RAFIKI_RESOURCE_URL=http://localhost:3000

# ============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================================================

# Host del servidor FastAPI
HOST=0.0.0.0

# Puerto del servidor FastAPI
PORT=8000

# Modo debug (true/false)
DEBUG=true

# URL del webhook (para que Rafiki nos notifique)
# Si FastAPI está en localhost y Rafiki en Docker, usar host.docker.internal
WEBHOOK_URL=http://host.docker.internal:8000/rafiki-webhook

# ============================================================================
# CONFIGURACIÓN DE AUTENTICACIÓN (Para producción)
# ============================================================================

# Secret key para JWT (¡CAMBIAR EN PRODUCCIÓN!)
SECRET_KEY=dev-secret-key-change-in-production

# Algoritmo de encriptación
ALGORITHM=HS256

# Tiempo de expiración del token (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# CONFIGURACIÓN DE BASE DE DATOS (Para producción con PostgreSQL)
# ============================================================================

# URL de conexión a la base de datos
# Formato: postgresql://user:password@localhost:5432/database_name
# DATABASE_URL=postgresql://constructoken:password@localhost:5432/constructoken_db

# Para el prototipo, usamos SQLite o en memoria
DATABASE_URL=sqlite:///./constructoken.db

# ============================================================================
# CONFIGURACIÓN DE LOGGING
# ============================================================================

# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# NOTAS
# ============================================================================

# 1. Este archivo es un ejemplo. Crea una copia llamada .env con tus valores reales.
# 2. NO subas el archivo .env a Git (está en .gitignore)
# 3. En producción, usa variables de entorno del sistema o un servicio de secrets
